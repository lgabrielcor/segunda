import React from "react";
import { LoginButton } from "@/components/login-btn";

function Company() {
  return (
    <div>
      <LoginButton />
      info de empresas
    </div>
  );
}

export default Company;
